<?xml version="1.0" encoding="utf-8"?>
<!-- Text longer than its lines scrolls up through the rest and starts over. The pause comes first,
     the scroll runs at a steady pace per pixel, the text holds at the end for as long as the
     visible lines took to pass, then it snaps back. -->
<component name="VerticalMarquee" extends="Group">
  <children>
    <Group id="viewport">
      <Label id="text" wrap="true" />
    </Group>
    <Timer id="pauseTimer" repeat="false" />
    <Timer id="holdTimer" repeat="false" />
    <Animation id="scroll" repeat="false" easeFunction="linear">
      <Vector2DFieldInterpolator id="scrollInterp" key="[0.0, 1.0]" fieldToInterp="text.translation" />
    </Animation>
  </children>
  <interface>
    <!-- { text, width, lines, fontSize, color, msPerPixel, pauseMs } -->
    <field id="model" type="assocarray" onChange="onModelChanged" />
    <field id="viewportHeight" type="float" />
  </interface>
</component>
